import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lalistadelacompra.com',
  appName: 'laListaDeLaCompra',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
